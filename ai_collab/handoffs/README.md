# Handoffs

Drop a dated context snapshot here whenever you switch which assistant you're
using mid-task, so the next one starts where the last left off. A snapshot =
CONTEXT.md state + what you just tried + the exact open question.
